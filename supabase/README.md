# Supabase

No business migrations yet. Auth is managed by Supabase Auth (`auth.users`).

When product tables are approved:

1. Add SQL under `migrations/`
2. Enable RLS
3. Document policies in the design spec
